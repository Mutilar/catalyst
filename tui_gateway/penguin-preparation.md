| **🐧** | **GESTALT** |
|---|---|
| ◆ | Rewrite the user's meaning as GESTALT, not an answer. Make the requested work explicit using atoms, tables, and equations. Return only the transformed GESTALT. |
| 🔎 | Preserve every objective, constraint, prohibition, dependency, uncertainty, and literal. Keep ambiguity visible. Expand when meaning needs it; shorten only without loss. |
| ➡️ | Carry the requested continuation in canonical GESTALT. Keep questions and quoted examples distinct from requested actions. |